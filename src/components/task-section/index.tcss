.task-section-horizontal {
  display: flex;
  width: max-content;
  padding: 0 10px;
}

.task-section-container {
  overflow-x: auto;
}

.task-section-container::-webkit-scrollbar {
  display: none;
}

.task-section-vertical {
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-column-gap: 10px;
  grid-row-gap: 10px;
}